package com.bubun.bubunapi.enums;

public enum ActivityEntityType {
    ACCOUNT,
    ACCOUNT_TRANSACTION,
    CATEGORY,
    CONTACT,
    GROUP,
    GROUP_EXPENSE,
    SETTLEMENT,
    TAG,
    USER,
    USER_EXPENSE
}
