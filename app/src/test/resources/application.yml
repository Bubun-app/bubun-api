server.port: 8081

bubun.url: ${BUBUN_URL}

spring:
  profiles.active: test
  datasource:
    url: jdbc:h2:mem:bubun-api-test
    username: root
    password: root
  h2.console.enabled: true
  jpa.hibernate.ddl-auto: create-drop